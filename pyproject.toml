[tool.poetry]
name = "nada"
version = "0.1.0"
description = ""
authors = []
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
zensical = ">=0.0.51"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
